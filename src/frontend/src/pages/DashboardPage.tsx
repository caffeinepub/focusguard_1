import Dashboard from '../components/Dashboard';

export default function DashboardPage() {
  return (
    <div className="container py-8 bg-gradient-to-b from-background via-muted/10 to-background min-h-[calc(100vh-8rem)]">
      <Dashboard />
    </div>
  );
}
